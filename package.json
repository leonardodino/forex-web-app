{
  "name": "forex-web-app",
  "version": "0.0.0-alpha",
  "private": true,
  "scripts": {
    "format": "prettier --write '**.{js,json,css,md}' --ignore-path .gitignore",
    "check": "prettier --check '**.{js,json,css,md}' --ignore-path .gitignore",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  },
  "dependencies": {
    "@rehooks/online-status": "^1.0.0",
    "dinero.js": "^1.5.0",
    "minireset.css": "^0.0.4",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1",
    "redux-actions": "^2.6.4",
    "rifm": "^0.7.0",
    "styled-components": "^4.1.3",
    "use-interval": "^1.1.0",
    "use-react-router": "^1.0.3"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.4",
    "prettier": "1.16.4",
    "react-scripts": "2.1.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
